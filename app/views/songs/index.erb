<head>
  <title>All Songs</title>
</head>

<body>

<div class="container-fluid">

  <h1>All Songs</h1>
  <hr/>

  <% @songs.each do |song| %>
    <div class="song">
      <h3><a href="/songs/<%= song.id %>"><%= song.title %></a> <small>(by: <%= song.artist %>)</small></h3>
      <p>
        <a href="<%= song.url %>"><%= song.url %></a>
      </p>
    </div>
  <% end %>

</div>

</body>

</html>
